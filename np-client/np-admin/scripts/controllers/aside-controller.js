/*
* AsideController Module
*
* Description
*/
angular.module('AsideController', [])
.controller('AsideController', ['$scope', '$rootScope', '$modal', '$timeout', '$location', '$localStorage', 'appConfig',
  function ($scope, $rootScope, $modal, $timeout, $location, $localStorage, appConfig) {

    console.log('AsideController');
  }
]);